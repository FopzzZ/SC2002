package Boundary;

import Controller.*;

public class AdminConfigSettingUI {
    public void main() {
		boolean exit  = false;
		int choice;
		while (!exit){
			choice = InputController.getIntFromUser();
			switch (choice) {
                default:
                    break;
            }
        }
    }

    //create the functions below 
}